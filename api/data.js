const UPSTREAM='https://tyxnyjyrfzspwcfjpzus.supabase.co/functions/v1/cuhalide-atlas-public-data-v3',PUBLIC_ORIGIN='https://cuhalide-atlas-v3.vercel.app',RELEASE='3.0.2',PUBLIC_DATA_VERSION='2.16.0',CURRENT_REVISION='7',PHOTOPHYSICS_CONTRACT='1.3.0';
const RETRIES=3,ATTEMPT_TIMEOUT_MS=7000,RETRY_DELAY_MS=180;
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
async function fetchWithRetry(url,req){let lastError,lastResponse;for(let attempt=0;attempt<RETRIES;attempt+=1){try{const response=await fetch(url,{method:req.method,headers:{accept:req.headers.accept||'application/json','user-agent':req.headers['user-agent']||'CuHalide-Atlas-Vercel-Legacy-Data/50.2-pass-a-preview'},redirect:'follow',signal:AbortSignal.timeout(ATTEMPT_TIMEOUT_MS)});lastResponse=response;if(response.status<500&&response.status!==429||attempt===RETRIES-1)return response;try{await response.body?.cancel()}catch{}}catch(error){lastError=error;if(attempt===RETRIES-1)break}if(attempt<RETRIES-1)await sleep(RETRY_DELAY_MS*(attempt+1))}if(lastResponse)return lastResponse;throw lastError||new Error('Public data backend request failed')}
export default async function handler(req,res){if(!['GET','HEAD'].includes(req.method)){res.statusCode=405;res.setHeader('Allow','GET, HEAD');return res.end('Method Not Allowed')}try{const incoming=new URL(req.url,PUBLIC_ORIGIN),upstream=new URL(UPSTREAM);for(const[key,value]of incoming.searchParams.entries())upstream.searchParams.append(key,value);const response=await fetchWithRetry(upstream,req);res.statusCode=response.status;res.setHeader('Content-Type',response.headers.get('content-type')||'application/json; charset=utf-8');res.setHeader('Cache-Control','no-store');res.setHeader('X-Content-Type-Options','nosniff');res.setHeader('X-Robots-Tag','noindex, nofollow');res.setHeader('X-CuHalide-Public-Access','query-and-view');res.setHeader('X-CuHalide-Release',response.headers.get('x-cuhalide-release')||RELEASE);res.setHeader('X-CuHalide-Public-Data-Version',response.headers.get('x-cuhalide-public-data-version')||PUBLIC_DATA_VERSION);res.setHeader('X-CuHalide-Current-Curated-Revision',response.headers.get('x-cuhalide-current-curated-revision')||CURRENT_REVISION);res.setHeader('X-CuHalide-Photophysics-Contract',response.headers.get('x-cuhalide-photophysics-contract')||PHOTOPHYSICS_CONTRACT);res.setHeader('Warning','299 - Legacy /api/data route exposes only the minimized public query contract; prefer /api/public-data.');if(req.method==='HEAD')return res.end();return res.end(await response.text())}catch(error){console.error('[cuhalide-legacy-data-proxy-v50.2-pass-a-preview]',error);res.statusCode=503;res.setHeader('Content-Type','application/json; charset=utf-8');res.setHeader('Cache-Control','no-store');res.setHeader('Retry-After','30');res.setHeader('X-Content-Type-Options','nosniff');res.setHeader('X-Robots-Tag','noindex, nofollow');res.setHeader('X-CuHalide-Public-Access','query-and-view');res.setHeader('X-CuHalide-Release',RELEASE);res.setHeader('X-CuHalide-Public-Data-Version',PUBLIC_DATA_VERSION);res.setHeader('X-CuHalide-Current-Curated-Revision',CURRENT_REVISION);res.setHeader('X-CuHalide-Photophysics-Contract',PHOTOPHYSICS_CONTRACT);return res.end(JSON.stringify({error:'CuHalide Atlas public data service is temporarily unavailable.',release:RELEASE,version:PUBLIC_DATA_VERSION,photophysics_contract:PHOTOPHYSICS_CONTRACT,public_access:'query-and-view'}))}}