const PUBLIC_ORIGIN='https://cuhalide-atlas-v3.vercel.app',CONTENT_DATE='2026-08-19',RELEASE='3.0.2',CURRENT_REVISION='9',SITE='51',UI='51.0',PUBLICATION_STATE='prepublication-review';
const URLS=[{loc:`${PUBLIC_ORIGIN}/`,priority:'1.0'},{loc:`${PUBLIC_ORIGIN}/motifs`,priority:'0.9'}];
const xml=v=>String(v).replace(/[<>&'\"]/g,c=>({'<':'&lt;','>':'&gt;','&':'&amp;',"'":'&apos;','"':'&quot;'}[c]));
function commonHeaders(res){res.setHeader('X-Content-Type-Options','nosniff');res.setHeader('X-Robots-Tag','noindex, nofollow, noarchive');res.setHeader('X-CuHalide-Release',RELEASE);res.setHeader('X-CuHalide-Site-Version',SITE);res.setHeader('X-CuHalide-UI-Version',UI);res.setHeader('X-CuHalide-Current-Curated-Revision',CURRENT_REVISION);res.setHeader('X-CuHalide-Publication-State',PUBLICATION_STATE);res.setHeader('X-CuHalide-Sitemap-Scope','prepublication-non-enumerating')}
export default async function handler(req,res){if(!['GET','HEAD'].includes(req.method)){commonHeaders(res);res.statusCode=405;res.setHeader('Allow','GET, HEAD');return res.end('Method Not Allowed')}const body=`<?xml version="1.0" encoding="UTF-8"?>\n<!-- CuHalide Atlas prepublication-review sitemap. Record-level enumeration is intentionally withheld until formal release. -->\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n${URLS.map(u=>`  <url><loc>${xml(u.loc)}</loc><lastmod>${CONTENT_DATE}</lastmod><changefreq>weekly</changefreq><priority>${u.priority}</priority></url>`).join('\n')}\n</urlset>\n`;res.statusCode=200;res.setHeader('Content-Type','application/xml; charset=utf-8');res.setHeader('Cache-Control','no-store, max-age=0');commonHeaders(res);res.setHeader('X-CuHalide-Sitemap-URLs',String(URLS.length));if(req.method==='HEAD')return res.end();return res.end(body)}
