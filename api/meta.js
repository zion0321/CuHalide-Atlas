const META_UPSTREAM='https://tyxnyjyrfzspwcfjpzus.supabase.co/functions/v1/cuhalide-atlas-meta-v302-stable',CONTRACT_UPSTREAM='https://tyxnyjyrfzspwcfjpzus.supabase.co/functions/v1/cuhalide-atlas-runtime-contract-v1-public',PUBLIC_ORIGIN='https://cuhalide-atlas-v3.vercel.app',META_VERSION='48.5',CURRENT_REVISION='3';
const TOTAL_TIMEOUT_MS=12000,FIRST_ATTEMPT_TIMEOUT_MS=5000,RETRY_DELAY_MS=120,sleep=(ms)=>new Promise(r=>setTimeout(r,ms));
function contentTypeFor(requested,upstreamType){if(requested==='sitemap'||requested==='sitemap.xml')return'application/xml; charset=utf-8';if(requested==='robots'||requested==='robots.txt')return'text/plain; charset=utf-8';if(requested==='citation'||requested==='citation.cff'||requested==='cff')return'text/plain; charset=utf-8';return'application/json; charset=utf-8'}
async function fetchSnapshot(url,req){const started=Date.now();let last=null,lastError=null;for(let attempt=0;attempt<2;attempt+=1){const remaining=TOTAL_TIMEOUT_MS-(Date.now()-started);if(remaining<=250){if(last)return last;throw lastError||new Error('metadata timeout')}const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),attempt===0?Math.min(FIRST_ATTEMPT_TIMEOUT_MS,remaining):remaining);try{const response=await fetch(url,{method:req.method,headers:{accept:req.headers.accept||'*/*','user-agent':req.headers['user-agent']||`CuHalide-Atlas-Vercel-Meta-Proxy/${META_VERSION}`},redirect:'follow',signal:controller.signal}),body=req.method==='HEAD'?'':await response.text(),snap={status:response.status,headers:response.headers,body};last=snap;if(response.status<500||attempt===1)return snap}catch(error){lastError=error;if(attempt===1){if(last)return last;throw error}}finally{clearTimeout(timer)}await sleep(RETRY_DELAY_MS)}if(last)return last;throw lastError||new Error('Metadata backend request failed')}
export default async function handler(req,res){if(!['GET','HEAD'].includes(req.method)){res.statusCode=405;res.setHeader('Allow','GET, HEAD');return res.end('Method Not Allowed')}try{const incoming=new URL(req.url,PUBLIC_ORIGIN),requested=(incoming.searchParams.get('action')||incoming.searchParams.get('asset')||'health').toLowerCase(),isHealth=requested==='health'||requested==='health.json',upstream=isHealth?new URL(CONTRACT_UPSTREAM):new URL(META_UPSTREAM);if(isHealth)upstream.searchParams.set('action','health');else for(const[key,value]of incoming.searchParams.entries())upstream.searchParams.append(key,value);const snapshot=await fetchSnapshot(upstream,req);res.statusCode=snapshot.status;res.setHeader('Content-Type',contentTypeFor(requested,snapshot.headers.get('content-type')));res.setHeader('X-Content-Type-Options','nosniff');res.setHeader('Cache-Control',isHealth?'no-store':(snapshot.headers.get('cache-control')||'public, max-age=300, s-maxage=3600'));for(const header of['etag','x-cuhalide-release','x-cuhalide-site-version','x-cuhalide-current-curated-revision']){const value=snapshot.headers.get(header);if(value)res.setHeader(header,value)}res.setHeader('X-CuHalide-Meta-Version',META_VERSION);res.setHeader('X-CuHalide-Current-Curated-Revision',CURRENT_REVISION);if(req.method==='HEAD')return res.end();if(!isHealth)return res.end(snapshot.body);let base={};try{base=JSON.parse(snapshot.body)}catch{base={ok:false,status:'FAIL',error:'invalid runtime contract health response'}}base.gateway_meta_version=META_VERSION;res.statusCode=base.ok===true?200:503;return res.end(JSON.stringify(base))}catch(error){console.error('[cuhalide-meta-proxy]',error);res.statusCode=error?.name==='AbortError'?504:502;res.setHeader('Content-Type','application/json; charset=utf-8');res.setHeader('Cache-Control','no-store');res.setHeader('X-CuHalide-Meta-Version',META_VERSION);res.setHeader('X-CuHalide-Current-Curated-Revision',CURRENT_REVISION);return res.end(JSON.stringify({error:error?.name==='AbortError'?'CuHalide Atlas metadata backend timed out.':'CuHalide Atlas metadata backend is temporarily unavailable.',release:'3.0.2',gateway_meta_version:META_VERSION}))}}
